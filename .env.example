# Optional: set to use the SQLite API instead of localStorage (e.g. http://localhost:3001)
# VITE_API_URL=http://localhost:3001

# COLB image analysis: Option C (client-side Tesseract.js) is used. No API keys or endpoints required.
# Images are processed in the browser only; no data is sent to external services.
