import React from 'react'
import LcrForm1ABirthAvailable from './LcrForm1ABirthAvailable'

/** Annotation (Child Ack) uses the same LCR Form 1A (Birth-Available) content. */
export default function AnnotationChildAck(props) {
  return <LcrForm1ABirthAvailable {...props} />
}
