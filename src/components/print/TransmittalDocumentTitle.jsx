import React from 'react'

/** Centered “TRANSMITTAL” heading directly under the Iligan header rule (print/PDF spacing in CSS). */
export default function TransmittalDocumentTitle() {
  return <p className="transmittal-doc-title m-0">TRANSMITTAL</p>
}
